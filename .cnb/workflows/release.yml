$:
  tag_push:
    - runner:
        tags: cnb:arch:amd64
        cpus: 8
      stages:
        - name: build release binaries
          image: golang:1.25-bookworm
          script: bash .cnb/scripts/release-build.sh

        - name: create release
          type: git:release
          options:
            title: Seven Player ${CNB_BRANCH}
            descriptionFromFile: release/RELEASE_NOTES.md
            latest: true

        - name: upload release attachments
          image: cnbcool/attachments:latest
          settings:
            attachments:
              - release/seven-player-windows-amd64.exe
              - release/seven-player-linux-amd64
              - release/seven-player-linux-arm64
